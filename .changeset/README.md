# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).

Run `npx changeset` to record a change. Each changeset describes which packages
should bump and at what semver level, and feeds the generated changelog + release.
